// import { createLogger } from '../logger'
// import { cfg } from './config'

// export const logger = createLogger(cfg.logLevel, cfg.dev)

// TODO setup renderer logger (maybe log to main process?)
export const logger = console
